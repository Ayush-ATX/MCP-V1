[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src src/mahasamvaad_eval
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
norecursedirs = benchmarks reports data .pytest_cache docs venv311 .venv mahasamvaad-mcp
